#include <Web.h>

void StartParing(){
  //Start paring with bluetooth.
};


void GotWIFILogin(char* SSID, char* Password){
  //Got WIFI login. Check if it works. If it works, then start WIFI and end bluetooth. If it doesn't work then continue bluetooth.
}